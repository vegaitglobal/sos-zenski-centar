import { rgba } from 'polished';

export const color = {
  white: '#fff',
  whiteFaded: rgba('#fff', 0.5),
  black: '#000',
  blackFaded: rgba('#000', 0.5),
};

/* styled-components theme */
export const theme = {
  color,
};
